# Prompting notes

The planner uses one deterministic JSON schema.

Design goals:
- minimize conversation turns (ask for clarification only when required)
- prefer Google Maps when multiple stops are requested
- keep spoken summaries short for driving safety
